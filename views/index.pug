extends layout

block content
  div.section
    h1.title= title
    p.subtitle Welcome to #[em LocalLibrary], a very basic Express website developed as a tutorial example on the Mozilla Developer Network.

  div.section
    h1.title.is4 Dynamic content

    if error
      p Error getting dynamic content
    else
      p The library has the following record counts:

      ul
        li #[strong Books:] !{data.book_count}
        li #[strong Copies:] !{data.book_instance_count}
        li #[strong Available copies:] !{data.book_instance_available_count}
        li #[strong Author:] !{data.author_count}
        li #[strong Genre:] !{data.genre_count}